# 南极多模态搜推与表征学习系统：14周项目计划 (UKAHT)
[English Version (英文版本)](./project_plan_14_weeks_en.md)

本毕设项目规划书围绕核心学术架构设计：  
**南极图像 $\rightarrow$ 多模态表示模型 (CLIP & BLIP) $\rightarrow$ [领域自适应投影 (MLP Adapter), 特征融合检索 (Late Fusion), 检索统计评估 (MAP/nDCG)]**

项目采用**“学术研究实验（以 Jupyter Notebook 呈现）与工业展示原型（以 FastAPI + React 呈现）双轨制”**的发展路线，既保证了数据科学毕业设计的算法深度，又保留了完整可演示的交互式应用成果。

---

### **模块化算法设计（核心推理接口契约）**
为了实现算法的独立微调和系统的低耦合性，本系统在前期抽象并固化了以下底层推理接口：
1. `get_image_embedding(image_source) -> np.ndarray` （提取 512 维图像特征向量）
2. `get_text_embedding(text_query) -> np.ndarray` （提取 512 维查询文本特征向量）
3. `get_blip_captions_batch(images) -> List[str]` （图像自动描述生成）
4. `classify_images_batch(images) -> List[dict]` （图像零样本自动打标）

---

### **14周日程表**

#### **第一阶段：系统 MVP 骨架与数据设计（第 1-2 周）**
*目标：快速搭建出前后端打通的检索原型系统，并设计 SQLite 特征向量数据库结构。*
- **第 1 周：检索系统 MVP 骨架打通**
  - 搭建 React + Vite 前端和 FastAPI 后端开发环境。配置开发代理解决跨域请求。
  - 构建核心推理函数的初始 Mock 模拟版本，打通前端检索网格与详情抽屉的基本交互。
- **第 2 周：数据集审计与向量数据库设计**
  - 盘点南极科考图像数据集，剔除无效文件。
  - 在 SQLite 中建立元数据与特征向量（BLOB 格式存储）的数据库表 Schema。
  - 锁定核心算法接口（Input/Output）规范，统一跨平台路径访问。

#### **第二阶段：多模态基线接入与批量索引（第 3-5 周）**
*目标：接入真实的 CLIP 和 BLIP 模型，并对整个图像库完成向量提取和索引存储。*
- **第 3 周：多模态模型提取基线实现**
  - 实现基于真实 CLIP 模型的单图/文本向量提取函数，实现基于真实 BLIP 模型的图像描述生成函数。
- **第 4 周：大规模图像库批量向量索引（Ingestion）**
  - 编写多线程/批处理推理管道（Batch Size = 4）以及 SQLite 批量提交事务逻辑。
  - 运行录入脚本对南极多媒体图像库完成全局自动分类打标、生成描述并提取特征向量存入 SQLite。
- **第 5 周：基线语义检索系统稳定化**
  - 在后端稳定运行文本语义搜索、相似图像推荐等接口，对接前端口述历史文本过滤器，确保系统整体基线功能正确无误。

#### **第三阶段：多层感知机（MLP）投影适配器开发（第 6-8 周）**
*目标：设计并训练一个自定义的 MLP 投影头，利用 Triplet Loss 调整图像与文本的对齐表征空间。*
- **第 6 周：三元组数据集（Triplet Dataset）构建**
  - 整理并配对“南极照片-相关口述历史转录段落”，构建正样本对（Positive Pairs）。
  - 通过随机负采样构建不相关的文本/图像作为负样本（Negative Samples）。
  - 完成三元组数据生成管道（Anchor, Positive, Negative）。
- **第 7 周：投影适配器（MLP Adapter）模型训练**
  - 在 PyTorch 中实现 2 层多层感知机投影模型，自定义三元组损失函数（Triplet Loss with margin）。
  - 利用优化器在南极特定三元组数据集上微调 MLP 投影参数，使其学会纠正 CLIP 的领域偏移。
- **第 8 周：自适应向量对齐集成**
  - 保存训练好的适配器权重（`.pth`），并将其无缝嵌入到 `functions.py` 的特征提取层中，验证系统的向前兼容性。

#### **第四阶段：多模态融合与检索精度定量评估（第 9-10 周）**
*目标：开发图文混合检索特征计算方法，并使用统计学指标科学衡量检索推荐精度。*
- **第 9 周：多模态晚期特征融合算法开发**
  - 实现图像视觉向量与生成的描述文本向量的加权融合距离公式：$S_{final} = \alpha S_{visual} + (1-\alpha)S_{textual}$，允许动态调节融合比例。
- **第 10 周：黄金测试集评估实验（消融分析）**
  - 标注少量的南极典型检索测试样本组成测试集。
  - 运行评估代码，计算并对比基线检索系统、自适应适配器检索系统以及多模态融合系统在 **MAP (平均精度均值)** 和 **nDCG (归一化折损累计增益)** 指标上的差异，绘制 Precision-Recall 曲线。

#### **第五阶段：学术可视化与成果整理（第 11-13 周）**
*目标：特征分布的高维数据降维可视化，以及项目交付总结。*
- **第 11 周：特征空间高维降维可视化（t-SNE）**
  - 在 Jupyter Notebook 中编写特征分析代码，利用 **t-SNE / UMAP** 算法将高维特征空间投影为二维，绘制散点图，直观展现适配器训练前后特征聚类的分离度变化。
- **第 12 周：系统交互优化与展示系统完善**
  - 优化前端展示效果，并针对口述历史检索与相似物品推荐进行流畅度细节优化。
- **第 13 周：交付容器化配置 (Dockerization)**
  - 编写 `Dockerfile` 和 `docker-compose.yml` 配置文件，在纯 CPU、轻量化的 Debian 镜像中打包应用，确保成果跨平台直接运行交付。

#### **第六阶段：交付与报告（第 14 周）**
*目标：项目交付与毕设系统总结。*
- **第 14 周：全链路测试与文档移交**
  - 整理算法的实验报告数据，撰写毕设论文的“方法与系统实现”章节，正式移交代码库。
